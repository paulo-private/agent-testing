package com.example;

import java.util.logging.Logger;

public class PrOnPrFixture {

    private static final Logger logger = Logger.getLogger(PrOnPrFixture.class.getName());

    public void report(String message) {
        logger.info(message);
    }
}
